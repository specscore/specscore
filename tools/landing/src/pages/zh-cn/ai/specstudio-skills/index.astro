---
// SpecStudio Skills — Simplified Chinese translation of the dedicated landing page.
// Canonical campaign URL: specscore.md/zh-cn/ai/specstudio-skills/
// Product names (SpecStudio Skills, SpecScore, Claude Code, GitHub) stay English.
// Slash-command names stay English (they're literal command strings).
// Terminal / code-block content stays English.
import BaseLayout from '../../../../layouts/BaseLayout.astro';
import SiteHeader from '../../../../components/SiteHeader.astro';
import SiteFooter from '../../../../components/SiteFooter.astro';
---

<BaseLayout
  lang="zh-CN"
  title="SpecStudio Skills — 让你的 AI 代理真正遵循的规格说明。"
  description="免费的 Claude Code 插件，将随意的提示词转化为经过评分的结构化规格说明——代理在写代码之前先读取它们。基于开放的 SpecScore 协议。"
  canonical="https://specscore.md/zh-cn/ai/specstudio-skills/"
>
  <SiteHeader />

  <main>
    {/* ── Section 1: Hero ── */}
    <section class="hero" aria-labelledby="hero-title">
      <div class="hero__inner">
        <h1 id="hero-title" class="hero__headline">
          让你的 AI 代理<br />真正遵循的规格说明。
        </h1>
        <p class="hero__sub">
          SpecStudio Skills 是一个免费的 Claude Code 插件，将随意的提示词
          转化为经过评分的结构化规格说明。代理在写任何代码之前先读取它们。
          基于开放的
          <a class="xref" href="https://specscore.md">SpecScore</a> 协议。
        </p>
        <div class="hero__install" id="install">
<pre class="terminal"><span class="prompt">$</span> /plugin marketplace add specscore/ai-marketplace
<span class="prompt">$</span> /plugin install specstudio@specscore</pre>
        </div>
        <p class="hero__demo-link">
          <a href="#demo" class="xref">观看 60 秒演示 ↓</a>
        </p>
      </div>
    </section>

    {/* ── Section 2: Demo ── */}
    <section class="section" id="demo" aria-labelledby="demo-title">
      <div class="section-inner">
        <h2 id="demo-title" class="section-title">
          从想法到评分规格，只需 60 秒。
        </h2>

        <div class="demo-placeholder">
          <div class="terminal-frame">
            <div class="terminal-frame__head">
              <span class="terminal-frame__title">Claude Code</span>
              <span class="terminal-frame__hint">演示</span>
            </div>
<pre class="terminal terminal-frame__body"><span class="prompt">&gt;</span> /specstudio:ideate A password reset flow for our SaaS app.

<span class="dim">Refining idea...</span>
<span class="dim">Producing spec/ideas/password-reset.md</span>

<span class="prompt">&gt;</span> /specstudio:specify spec/ideas/password-reset.md

<span class="dim">Writing Feature spec...</span>
<span class="dim">spec/features/password-reset/README.md</span>

<span class="prompt">$</span> specscore lint spec/features/password-reset/
<span class="mark-pass">PASS</span>  Feature: password-reset
  <span class="mark-pass">PASS</span>  has title, status, and description
  <span class="mark-pass">PASS</span>  requirements have acceptance criteria
  <span class="mark-pass">PASS</span>  all AC IDs are unique
  <span class="mark-pass">PASS</span>  no orphan cross-references

<span class="mark-pass">Score: 92 / 100</span></pre>
          </div>
          <p class="demo-caption">
            无剪辑。无拼接。纯粹的 Claude Code → 安装插件 →
            评分真实规格 → 代理精确执行你的意图。
          </p>
        </div>
      </div>
    </section>

    {/* ── Section 3: The wedge ── */}
    <section class="section" aria-labelledby="wedge-title">
      <div class="section-inner section-inner--prose">
        <h2 id="wedge-title" class="section-title">
          为什么你的代理一次又一次做错事。
        </h2>
        <p class="prose-p">
          随意的提示词有效——直到它们失效。代理只实现了功能的一半。
          误解了一个需求。凭空编造了一个不存在的 API。选了错误的库。
          你重新措辞提示词。然后再来一次。到第三次时，你其实
          已经在手动把规格敲进聊天窗口了。
        </p>
        <p class="prose-p">
          问题不在代理。问题在于"帮我做个功能"不是规格——那是个愿望。
          代理是随机的。愿望天生就是随机的。规格不是。
        </p>
        <p class="prose-p">
          SpecStudio Skills 提供了一种编写规格的工作流，代理可以真正
          解析它：结构化的功能、需求、验收标准——在代理看到规格之前
          就完成了完整性评分。和随意编码一样快，但结果的偏差大幅减小。
        </p>
      </div>
    </section>

    {/* ── Section 4: The pipeline ── */}
    <section class="section pipeline-section" aria-labelledby="pipeline-title">
      <div class="section-inner">
        <h2 id="pipeline-title" class="section-title">
          四个命令。一条流水线。
        </h2>
        <p class="pipeline-intro">
          安装一次，然后按顺序运行——从原始想法到可执行计划。
        </p>

        <div class="pipeline-grid">
          <div class="pipeline-card">
            <div class="pipeline-card__head">
              <span class="pipeline-card__cmd">/ideate</span>
            </div>
            <p class="pipeline-card__desc">
              通过结构化的发散和收敛思维打磨原始想法。
              生成一份可通过检查的单页预规格。
            </p>
          </div>

          <div class="pipeline-card">
            <div class="pipeline-card__head">
              <span class="pipeline-card__cmd">/specify</span>
            </div>
            <p class="pipeline-card__desc">
              将已批准的想法转化为带有需求、验收标准
              和 Given/When/Then 块的 SpecScore Feature。
            </p>
          </div>

          <div class="pipeline-card">
            <div class="pipeline-card__head">
              <span class="pipeline-card__cmd">/plan</span>
            </div>
            <p class="pipeline-card__desc">
              将功能分解为有序的任务列表。每个任务
              关联一个或多个验收标准。
            </p>
          </div>

          <div class="pipeline-card">
            <div class="pipeline-card__head">
              <span class="pipeline-card__cmd">/implement</span>
            </div>
            <p class="pipeline-card__desc">
              执行计划中的下一个任务。每个任务一个子代理，
              提交时附带验收标准追踪，staged diff 等你审批。
            </p>
          </div>
        </div>
      </div>
    </section>

    {/* ── Section 5: What's in the box ── */}
    <section class="section" aria-labelledby="box-title">
      <div class="section-inner section-inner--prose">
        <h2 id="box-title" class="section-title">
          包含什么。
        </h2>

        <h3 class="list-heading">能做的：</h3>
        <ul class="feature-list feature-list--does">
          <li>添加完整 SDD 周期的斜杠命令：<code>/ideate</code>、<code>/specify</code>、<code>/plan</code>、<code>/implement</code>。</li>
          <li>在代理看到规格之前，评估其完整性和模糊性。</li>
          <li>以开放的 SpecScore 格式编写规格——没有专有扩展。</li>
        </ul>

        <h3 class="list-heading">不做的：</h3>
        <ul class="feature-list feature-list--doesnt">
          <li>不替你运行代理。插件是工作流层；代理仍然是 Claude。</li>
          <li>不向服务器发送数据。规格和评分保留在本地。</li>
          <li>不会锁定你。如果你移除插件，你的规格仍然是有效的 Markdown，仍可通过 <a class="xref" href="https://github.com/specscore/specscore">specscore</a> CLI 检查。</li>
        </ul>

        <p class="sibling-callout">
          在找无主张的 CLI 包装？
          <a class="xref" href="https://github.com/specscore/ai-plugin-specscore">ai-plugin-specscore</a> 是
          轻量包装。SpecStudio Skills 是在其之上的主张明确的工作流。
        </p>
      </div>
    </section>

    {/* ── Section 6: Open underneath ── */}
    <section class="section" aria-labelledby="open-title">
      <div class="section-inner section-inner--prose">
        <h2 id="open-title" class="section-title">
          基于开放的 SpecScore 协议构建。
        </h2>
        <p class="prose-p">
          SpecStudio Skills 以
          <a class="xref" href="https://specscore.md">SpecScore</a> 格式编写
          规格——这是一个开放标准。格式是人类可读的 Markdown。Schema 已公开发布。
          CLI 是开源的。没有供应商锁定。如果你有一天不再使用 SpecStudio，
          你的规格会跟着你走。
        </p>
      </div>
    </section>
  </main>

  {/* ── Final CTA ── */}
  <section class="final-cta" aria-labelledby="cta-title">
    <div class="final-cta__inner">
      <h2 id="cta-title" class="final-cta__title">试一试。</h2>
      <div class="final-cta__install">
<pre class="terminal"><span class="prompt">$</span> /plugin marketplace add specscore/ai-marketplace
<span class="prompt">$</span> /plugin install specstudio@specscore</pre>
      </div>
      <p class="final-cta__note">
        对公开和开源仓库免费。MIT 许可证。
      </p>
    </div>
  </section>

  {/* ── Footer ── */}
  <footer class="page-footer">
    <div class="page-footer__inner">
      <div class="page-footer__col page-footer__brand">
        <a href="/zh-cn/" class="wordmark" aria-label="SpecScore 首页">
          <span class="wordmark__clef" aria-hidden="true">&#x1D11E;</span>
          <span class="wordmark__name">SpecScore.md</span>
        </a>
        <p class="page-footer__tagline">
          用于规范、需求和验收标准的开放标准。
        </p>
        <p class="page-footer__copyright">
          &copy; 2026 SpecScore contributors. MIT license.
        </p>
      </div>

      <div class="page-footer__col">
        <h4 class="page-footer__heading">SpecStudio Skills</h4>
        <ul class="page-footer__list">
          <li><a href="https://github.com/specscore/specstudio-skills">GitHub</a></li>
          <li><a href="https://github.com/specscore/specstudio-skills/blob/main/CHANGELOG.md">Changelog</a></li>
          <li><a href="https://github.com/specscore/specstudio-skills/blob/main/LICENSE">许可证：MIT</a></li>
        </ul>
      </div>

      <div class="page-footer__col">
        <h4 class="page-footer__heading">生态系统</h4>
        <ul class="page-footer__list">
          <li><a href="https://specscore.md">SpecScore</a> — 开放标准</li>
          <li><a href="https://github.com/specscore/ai-plugin-specscore">ai-plugin-specscore</a> — 轻量 CLI 包装</li>
          <li><a href="https://specscore.studio">SpecScore Studio</a> — Web 查看器</li>
        </ul>
      </div>
    </div>
  </footer>
</BaseLayout>

<style>
  /* ── Hero ── */
  .hero {
    padding: var(--space-7) var(--space-3) var(--space-6);
    background: var(--cream);
  }
  .hero__inner {
    max-width: var(--max-prose);
    margin: 0 auto;
    animation: fade-in 600ms cubic-bezier(0.22, 0.61, 0.36, 1) both 180ms;
  }
  .hero__headline {
    font-size: var(--fs-hero);
    font-weight: 700;
    line-height: 1.04;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-3);
  }
  .hero__sub {
    font-size: 1.125rem;
    color: var(--mid-ink);
    margin-bottom: var(--space-4);
    line-height: 1.55;
  }
  .hero__install {
    margin-bottom: var(--space-3);
  }
  .hero__demo-link {
    font-size: var(--fs-small);
    color: var(--mid-ink);
  }

  @keyframes fade-in {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── Section titles ── */
  .section-title {
    font-size: var(--fs-h2);
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-bottom: var(--space-3);
  }

  /* ── Demo ── */
  .demo-placeholder {
    margin-top: var(--space-3);
  }
  .demo-caption {
    margin-top: var(--space-2);
    font-size: var(--fs-small);
    color: var(--mid-ink);
  }

  .terminal-frame {
    border: 1px solid var(--hairline);
    background: var(--surface);
  }
  .terminal-frame__head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.75rem;
    padding: 0.5rem 0.875rem;
    border-bottom: 1px solid var(--hairline);
    background: var(--cream-2);
    font-family: var(--font-code);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .terminal-frame__title { color: var(--graphite); font-weight: 700; }
  .terminal-frame__hint  { color: var(--mid-ink); }
  .terminal-frame__body  { border: none !important; }

  /* ── Wedge (prose sections) ── */
  .prose-p {
    color: var(--mid-ink);
    line-height: 1.65;
    margin-bottom: var(--space-3);
  }
  .prose-p:last-child {
    margin-bottom: 0;
  }

  /* ── Pipeline grid ── */
  .pipeline-section {
    background: var(--cream-2);
  }
  .pipeline-intro {
    color: var(--mid-ink);
    margin-bottom: var(--space-4);
    line-height: 1.6;
  }
  .pipeline-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-3);
  }
  .pipeline-card {
    background: var(--surface);
    border: 1px solid var(--hairline);
    padding: var(--space-3);
  }
  .pipeline-card__head {
    margin-bottom: var(--space-2);
  }
  .pipeline-card__cmd {
    font-family: var(--font-code);
    font-size: 1rem;
    font-weight: 600;
    color: var(--ink);
  }
  .pipeline-card__desc {
    font-size: var(--fs-small);
    color: var(--mid-ink);
    line-height: 1.55;
  }

  @media (max-width: 860px) {
    .pipeline-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 520px) {
    .pipeline-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ── What's in the box ── */
  .list-heading {
    font-family: var(--font-display);
    font-size: var(--fs-h3);
    font-weight: 600;
    margin-top: var(--space-3);
    margin-bottom: var(--space-2);
  }
  .list-heading:first-of-type {
    margin-top: 0;
  }
  .feature-list {
    margin: 0 0 var(--space-3) 0;
    padding-left: 1.25rem;
    color: var(--mid-ink);
    line-height: 1.65;
  }
  .feature-list li {
    margin-bottom: 0.5rem;
  }
  .feature-list li:last-child {
    margin-bottom: 0;
  }
  .feature-list code {
    font-size: 0.9em;
    background: var(--surface);
    border: 1px solid var(--hairline);
    padding: 0.1em 0.35em;
    border-radius: 2px;
  }
  .sibling-callout {
    margin-top: var(--space-4);
    padding: var(--space-2) var(--space-3);
    border-left: 3px solid var(--hairline);
    font-size: var(--fs-small);
    color: var(--mid-ink);
    line-height: 1.55;
  }

  /* ── Final CTA ── */
  .final-cta {
    background: var(--cream-2);
    padding: var(--space-7) var(--space-3);
    border-top: 1px solid var(--hairline);
    border-bottom: 1px solid var(--hairline);
  }
  .final-cta__inner {
    max-width: var(--max-prose);
    margin: 0 auto;
    text-align: center;
  }
  .final-cta__title {
    font-size: clamp(2.2rem, 4.2vw, 3.2rem);
    margin-bottom: var(--space-4);
    letter-spacing: -0.02em;
    line-height: 1.05;
  }
  .final-cta__install {
    display: inline-block;
    text-align: left;
    margin-bottom: var(--space-3);
  }
  .final-cta__note {
    color: var(--mid-ink);
    font-size: var(--fs-small);
  }

  /* ── Page footer ── */
  .page-footer {
    background: var(--cream);
    padding: var(--space-6) var(--space-3) var(--space-5);
  }
  .page-footer__inner {
    max-width: var(--max-content);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: var(--space-4);
  }
  .page-footer__brand .wordmark {
    margin-bottom: var(--space-2);
  }
  .page-footer__tagline {
    color: var(--mid-ink);
    margin-bottom: var(--space-2);
    max-width: 36ch;
    line-height: 1.5;
    font-size: 0.95rem;
  }
  .page-footer__copyright {
    color: var(--pale-ink);
    font-size: 0.825rem;
    font-family: var(--font-code);
  }
  .page-footer__heading {
    font-family: var(--font-display);
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--mid-ink);
    margin-bottom: var(--space-2);
  }
  .page-footer__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .page-footer__list a {
    color: var(--ink);
    font-size: 0.9rem;
    border-bottom: 1px solid transparent;
    transition: border-color 120ms ease;
  }
  .page-footer__list a:hover {
    border-bottom-color: var(--ink);
  }
  .page-footer__list a:focus-visible {
    outline: 2px solid var(--ink-blue);
    outline-offset: 2px;
    border-radius: 2px;
  }

  @media (max-width: 800px) {
    .page-footer__inner {
      grid-template-columns: 1fr 1fr;
    }
    .page-footer__brand {
      grid-column: 1 / -1;
    }
  }
</style>
