---
// Japanese variant of LinterDemo.astro. Terminal content stays English.
import LintExample from './LintExample.astro';
---

<section class="linter section" aria-labelledby="linter-title">
  <div class="section-inner">
    <h2 id="linter-title" class="linter__title">リンターを実行。</h2>
    <p class="linter__intro">
      先ほどの仕様に対して<code>specscore lint</code>を実行します。
      出力結果はこちらです。
    </p>

    <div
      class="linter__terminal"
      role="img"
      aria-label="specscore lintのターミナル出力：R1とR2が緑のチェックで通過、R3が赤のマークで失敗し、修正可能なエラーメッセージが表示。"
    >
<LintExample />
    </div>
  </div>
</section>

<style>
  .linter { background: var(--cream); }
  .linter__title {
    font-size: var(--fs-h2);
    margin-bottom: var(--space-2);
    font-weight: 700;
    letter-spacing: -0.01em;
  }
  .linter__intro {
    color: var(--mid-ink);
    margin-bottom: var(--space-4);
  }
  .linter__intro code {
    background: var(--cream-2);
    padding: 0.05em 0.35em;
    border-radius: 2px;
    font-size: 0.92em;
    color: var(--ink);
  }
  .linter__terminal {
    margin-bottom: var(--space-3);
  }
</style>
