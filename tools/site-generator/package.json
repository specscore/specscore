{
  "name": "specscore-site-generator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf ../../public",
    "test": "node --test lib/**/*.test.js"
  },
  "dependencies": {
    "markdown-it": "^14.0.0"
  }
}
