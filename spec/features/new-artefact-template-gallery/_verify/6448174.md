```yaml
feature: new-artefact-template-gallery
revision: 6448174
verdicts:
  - ac: new-artefact-template-gallery#ac:idea-template-published
    verdict: pass
    justification: "Built public/new/idea.md is byte-identical to source new/idea.md (cmp clean, 1289 bytes) and served as .md; build.js writes the source verbatim."
  - ac: new-artefact-template-gallery#ac:index-lists-types
    verdict: pass
    justification: "Built /new/index.html lists all 7 artefact types, each <li> linking to /new/<type>.md; all 7 template files published alongside."
  - ac: new-artefact-template-gallery#ac:supported-set-present
    verdict: pass
    justification: "Site builds and publishes /new/<type>.md for all seven required types: idea, feature, plan, task, decision, issue, proposal."
  - ac: new-artefact-template-gallery#ac:templates-outside-spec-tree
    verdict: pass
    justification: "specscore spec lint returns 0 violations at HEAD; no new/ template file is reported as an artefact violation and the tree is clean."
  - ac: new-artefact-template-gallery#ac:template-is-empty-skeleton
    verdict: pass
    justification: "new/idea.md holds canonical Idea headings plus <!-- … --> prompts and only placeholder tokens; no filled-in real content. Frontmatter is metadata."
  - ac: new-artefact-template-gallery#ac:filled-template-lints-clean
    verdict: pass
    justification: "Filled new/idea.md (frontmatter retained) lints clean — only residual was the index-completeness entry for the throwaway slug, which cleared on adding the index row."
  - ac: new-artefact-template-gallery#ac:templates-carry-format
    verdict: pass
    justification: "All 7 new/<type>.md carry format: https://specscore.md/<type>-specification; the 5 with footers have footer URL == format value; task/proposal have no footer."
  - ac: new-artefact-template-gallery#ac:status-bearing-templates-mirror-status
    verdict: pass
    justification: "All 5 templates' frontmatter status equals their body **Status:** token (idea/feature/plan/proposal=Draft, decision=Proposed); plan's pending tokens are per-task."
  - ac: new-artefact-template-gallery#ac:task-template-format-only
    verdict: pass
    justification: "new/task.md frontmatter has format: …/task-specification; grep for status:/Status: yields no match anywhere in the file."
```

## AC: idea-template-published

**Verdict:** pass

**Justification:** Built `public/new/idea.md` is byte-identical to source `new/idea.md` (`cmp -s` clean, 1289 bytes) and retains the `.md` extension (served as Markdown by the static host). `build.js` reads the source and writes it verbatim with no transformation, so the published copy tracks the source even after the frontmatter addition.

**Commits:**
- e4f2cb7 — feat(site): publish new-artefact templates at /new/

**Evidence:**
- new/idea.md (source, 1289 bytes, includes YAML frontmatter)
- public/new/idea.md (build output, `cmp -s` byte-identical)
- tools/site-generator/build.js @ e4f2cb7 (reads source, writes byte-for-byte to /new/<file>.md)

## AC: index-lists-types

**Verdict:** pass

**Justification:** The built `/new/index.html` lists all 7 supported artefact types (decision, feature, idea, issue, plan, proposal, task), and each entry is an `<li><a href="/new/<type>.md">` link to its template; all 7 template `.md` files are published alongside.

**Commits:**
- e4f2cb7 — feat(site): publish new-artefact templates at /new/

**Evidence:**
- tools/site-generator/build.js @ e4f2cb7 (emits `<li><a href="/new/${e.file}">` per template)
- public/new/index.html (hrefs to /new/{decision,feature,idea,issue,plan,proposal,task}.md)

## AC: supported-set-present

**Verdict:** pass

**Justification:** The site build (exit 0) publishes a template at `/new/<type>.md` for each of the seven required types: idea, feature, plan, task, decision, issue, proposal.

**Commits:**
- e4f2cb7 — feat(site): publish new-artefact templates at /new/

**Evidence:**
- commit e4f2cb7 adds new/{idea,feature,plan,task,decision,issue,proposal}.md
- public/new/ contains all 7 `<type>.md` files after `node tools/site-generator/build.js`

## AC: templates-outside-spec-tree

**Verdict:** pass

**Justification:** `specscore spec lint` returns "0 violations found" (exit 0) at HEAD; no `new/` template file is reported as an artefact violation, and the lint tree stays clean even with the templates' new YAML frontmatter.

**Commits:**
- e4f2cb7 — feat(site): publish new-artefact templates at /new/

**Evidence:**
- templates placed under new/ (outside spec/) per commit e4f2cb7
- `specscore spec lint` output: "0 violations found" (exit 0), no new/ references
- HEAD 6448174 (merge of frontmatter PR #14)

## AC: template-is-empty-skeleton

**Verdict:** pass

**Justification:** `new/idea.md` contains the canonical Idea section headings (Problem Statement, Context, Recommended Direction, Alternatives Considered, MVP Scope, Not Doing, Key Assumptions to Validate, SpecScore Integration, Open Questions) and `<!-- … -->` prompts, with only placeholder tokens and no filled-in real content. The added frontmatter is metadata, not content.

**Commits:**
- e4f2cb7 — feat(site): publish new-artefact templates at /new/

**Evidence:**
- new/idea.md (canonical headings + `<!-- … -->` prompts + `<...>` / … placeholders only)

## AC: filled-template-lints-clean

**Verdict:** pass

**Justification:** A filled copy of `new/idea.md` (frontmatter retained), saved at a throwaway `spec/ideas/<slug>.md`, produced only the `idea-index-completeness` violation for the throwaway slug; adding the index row yielded "0 violations found". The artefact content lints clean; the working tree was left clean.

**Commits:**
- e4f2cb7 — feat(site): publish new-artefact templates at /new/

**Evidence:**
- spec/ideas/zz-verify-fill.md (throwaway filled copy, removed after run)
- pre-index-row lint: only `idea-index-completeness: missing entries: zz-verify-fill`
- post-index-row lint: "0 violations found"
- git status clean after cleanup

## AC: templates-carry-format

**Verdict:** pass

**Justification:** All 7 `new/<type>.md` carry `format: https://specscore.md/<type>-specification`; the 5 templates with a footer line (decision, idea, issue, feature, plan) have footer URL == `format:` value; task and proposal have no footer, so the footer-match clause does not apply.

**Commits:**
- 0d72667 — feat(new-artefact-template-gallery): add frontmatter to /new/* templates

**Evidence:**
- `grep '^format:' new/*.md` — 7 matches, each `https://specscore.md/<type>-specification`
- `grep 'This document follows' new/*.md` — 5 footers, each URL equals its file's `format:`; no footer in task.md/proposal.md

## AC: status-bearing-templates-mirror-status

**Verdict:** pass

**Justification:** Each of the 5 status-bearing templates carries a frontmatter `status:` equal to its body `**Status:**` token — idea/feature/plan/proposal = Draft, decision = Proposed. The `pending` tokens in plan.md are per-task statuses, not the plan artefact's own status (which is Draft).

**Commits:**
- 0d72667 — feat(new-artefact-template-gallery): add frontmatter to /new/* templates

**Evidence:**
- new/idea.md — `status: Draft` / `**Status:** Draft`
- new/feature.md — Draft / Draft
- new/plan.md — `status: Draft` / `**Status:** Draft` (per-task `pending` lines distinct)
- new/decision.md — Proposed / Proposed
- new/proposal.md — Draft / Draft

## AC: task-template-format-only

**Verdict:** pass

**Justification:** `new/task.md` carries a frontmatter `format: https://specscore.md/task-specification` and no `status:` key anywhere, and no body `**Status:**` line — matching its board-managed status model.

**Commits:**
- 0d72667 — feat(new-artefact-template-gallery): add frontmatter to /new/* templates

**Evidence:**
- new/task.md frontmatter: `format: https://specscore.md/task-specification`
- grep of new/task.md: only `format:` matched; no `status:` key, no body `**Status:**`

---
*This document follows the https://specscore.md/index-specification*
