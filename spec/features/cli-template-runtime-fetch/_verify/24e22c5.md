```yaml
feature: cli-template-runtime-fetch
revision: 24e22c5
verdicts:
  - ac: cli-template-runtime-fetch#ac:fetches-published-template
    verdict: pass
    justification: "Bare `idea new` calls bareOrEmbedded → fetchTemplate(\"idea\") issuing GET <base>/new/idea.md before os.WriteFile; TestIdeaNew_FetchesPublishedTemplate asserts the path and passes."
  - ac: cli-template-runtime-fetch#ac:maps-type-to-url
    verdict: pass
    justification: "templateBaseURL() reads SPECSCORE_TEMPLATE_BASE_URL; fetchTemplate(\"feature\") builds <base>/new/feature.md; TestFetchTemplate_MapsTypeToURL asserts /new/feature.md and passes."
  - ac: cli-template-runtime-fetch#ac:fills-known-fields
    verdict: pass
    justification: "Substitution maps <Idea Name>→\"My Idea\", YYYY-MM-DD→today, <your-handle>→alex; an AC-exact run produced `# Idea: My Idea`, `**Date:** <today>`, `**Owner:** alex`."
  - ac: cli-template-runtime-fetch#ac:falls-back-when-offline
    verdict: pass
    justification: "bareOrEmbedded returns embeddedFn() on any fetch failure; runIdeaNew writes it and returns nil (exit 0). TestIdeaNew_FallsBackWhenOffline (127.0.0.1:0) passes."
  - ac: cli-template-runtime-fetch#ac:timeout-bounds-the-wait
    verdict: pass
    justification: "fetchTemplate uses context.WithTimeout(templateFetchTimeout=3s); on timeout → embedded fallback. TestFetchTemplate_TimesOut (never-responding server) aborts <1s and passes."
  - ac: cli-template-runtime-fetch#ac:warns-on-fallback
    verdict: pass
    justification: "On fallback bareOrEmbedded prints exactly one Fprintln(stderr, fellBackWarning); command writes file and returns nil. TestIdeaNew_FallsBackWhenOffline asserts stderr warning + exit 0."
  - ac: cli-template-runtime-fetch#ac:fetch-and-fallback-parity
    verdict: pass
    justification: "Both paths share title/date/owner defaults; a fetch-vs-offline run produced identical Title/Date/Owner and both passed the command's built-in lint gate (errors if not clean)."
```

# Verify Report — cli-template-runtime-fetch @ 24e22c5

**Summary:** 7 passed, 0 failed, 0 unmapped, 0 errored (7 ACs total).

> **Cross-repo note.** This Feature's spec lives in the `specscore` repo, but its
> implementation and `Verifies:` commit trailers live in **`specscore-cli`**.
> The mapping commit for every AC is `specscore-cli@9d79543`
> ("feat(cli): fetch new-artefact templates from the gallery, fall back offline",
> the squash-merge of PR specscore-cli#52). Verifier subagents inspected that
> commit and the `specscore-cli` working tree.

## AC: fetches-published-template

**Verdict:** pass

**Justification:** Bare `idea new` calls `bareOrEmbedded(bare=true, "idea", …)` → `fetchTemplate("idea")`, which issues `GET <base>/new/idea.md` via `templateHTTPClient.Do` before `os.WriteFile(target, body)`. The fetch precedes the write.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/template_fetch.go` — `fetchTemplate` builds `<base>/new/<typ>.md` and GETs it
- `internal/cli/idea.go` — `runIdeaNew` computes `bare`, calls `bareOrEmbedded`, then `os.WriteFile`
- `internal/cli/template_fetch_test.go` — `TestIdeaNew_FetchesPublishedTemplate` asserts `gotPath == "/new/idea.md"` (PASS)

## AC: maps-type-to-url

**Verdict:** pass

**Justification:** `templateBaseURL()` reads `SPECSCORE_TEMPLATE_BASE_URL` (default `https://specscore.md`); `fetchTemplate("feature")` builds `<base>/new/feature.md`; a bare `feature new` calls `resolveBareTemplate("feature", …)`.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/template_fetch.go` — `templateBaseURL` + `fetchTemplate` URL construction
- `internal/cli/feature.go` — `runFeatureNew` calls `resolveBareTemplate("feature", …)`
- `internal/cli/template_fetch_test.go` — `TestFetchTemplate_MapsTypeToURL` asserts `/new/feature.md` (PASS)

## AC: fills-known-fields

**Verdict:** pass

**Justification:** Substitution maps `<Idea Name>`→title (`My Idea` from slug `my-idea`), `YYYY-MM-DD`→today's UTC date, `<your-handle>`→owner (`alex`). An AC-exact run (`idea new my-idea --owner alex`) produced `# Idea: My Idea`, `**Date:** <today>`, `**Owner:** alex`.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/template_fetch.go` — `resolveBareTemplate` substitution; `templateTitleOrSlug` / `templateTodayUTC` / `templateOwnerOrUnknown`
- `internal/cli/idea.go` — repl map wiring `<Idea Name>` / `YYYY-MM-DD` / `<your-handle>`
- `internal/cli/template_fetch_test.go` — `TestIdeaNew_FetchesPublishedTemplate`, `TestResolveBareTemplate` (PASS); plus an empirical AC-exact run

## AC: falls-back-when-offline

**Verdict:** pass

**Justification:** `bareOrEmbedded` returns `embeddedFn()` on any fetch failure (unreachable base URL → `ok=false`); `runIdeaNew` writes that body and returns `nil` (exit 0). `TestIdeaNew_FallsBackWhenOffline` (base `http://127.0.0.1:0`) asserts `err==nil` and that the embedded-template file exists.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/template_fetch.go` — `resolveBareTemplate` `ok=false` on any error; `bareOrEmbedded` falls to `embeddedFn`
- `internal/cli/idea.go` — `runIdeaNew`: `bareOrEmbedded` → `os.WriteFile` → `return nil`
- `internal/cli/template_fetch_test.go` — `TestIdeaNew_FallsBackWhenOffline` (PASS)

## AC: timeout-bounds-the-wait

**Verdict:** pass

**Justification:** `fetchTemplate` uses `context.WithTimeout(templateFetchTimeout)` (default 3s); on timeout it returns an error → `resolveBareTemplate` `ok=false` → embedded fallback. `TestFetchTemplate_TimesOut` (never-responding server) aborts in ~0.15s under a lowered deadline and asserts the abort.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/template_fetch.go` — `var templateFetchTimeout = 3 * time.Second`; `context.WithTimeout` in `fetchTemplate`
- `internal/cli/template_fetch_test.go` — `TestFetchTemplate_TimesOut` (blocking server, asserts elapsed < 1s) (PASS)

## AC: warns-on-fallback

**Verdict:** pass

**Justification:** On an unreachable gallery, `bareOrEmbedded` prints exactly one `fmt.Fprintln(errw, fellBackWarning)` line to stderr, then `runIdeaNew` writes the file and returns `nil` (exit 0). No warning on a successful fetch.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/template_fetch.go` — single `Fprintln(errw, fellBackWarning)` on `!ok`; `fellBackWarning = "warning: specscore.md unreachable, used built-in template"`
- `internal/cli/idea.go` — passes `cmd.ErrOrStderr()` as `errw`, then writes the file and returns `nil`
- `internal/cli/template_fetch_test.go` — `TestIdeaNew_FallsBackWhenOffline` asserts the stderr warning + exit 0 (PASS)

## AC: fetch-and-fallback-parity

**Verdict:** pass

**Justification:** Both paths share the same title/date/owner defaults (owner→`$USER`/`unknown`, title→title-cased slug, date→`time.Now().UTC()`). A direct fetch-vs-offline run on identical inputs produced identical `Title="Same Idea"`, `Date`, `Owner="alex"`, and both passed the command's built-in lint gate (the command errors if the created file is not lint-clean). Per the (softened) AC, section-body wording may differ.

**Commits:**
- specscore-cli@9d79543 — feat(cli): fetch new-artefact templates from the gallery, fall back offline

**Evidence:**
- `internal/cli/idea.go` — bare-or-embedded selection + the internal lint gate that errors if not clean
- `internal/cli/template_fetch.go` — `templateTitleOrSlug` / `templateOwnerOrUnknown` / `templateTodayUTC` mirror the embedded defaults
- `pkg/idea/scaffold.go` — embedded scaffolder defaults (title-cased slug, owner `unknown`, UTC date)
- Empirical fetch-vs-offline parity run: identical Title/Date/Owner, both lint-clean
